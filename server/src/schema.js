export const SCHEMA = [
  `CREATE TABLE IF NOT EXISTS users (id TEXT PRIMARY KEY, username TEXT NOT NULL COLLATE NOCASE UNIQUE, email TEXT NOT NULL COLLATE NOCASE UNIQUE, password_hash TEXT NOT NULL, role TEXT NOT NULL DEFAULT 'PLAYER' CHECK(role IN ('PLAYER','ADMIN')), status TEXT NOT NULL DEFAULT 'ACTIVE' CHECK(status IN ('ACTIVE','SUSPENDED')), force_password_change INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, last_login_at TEXT)`,
  `CREATE TABLE IF NOT EXISTS profiles (user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE, display_name TEXT NOT NULL, avatar TEXT NOT NULL DEFAULT 'orb-cyan', bio TEXT NOT NULL DEFAULT '', level INTEGER NOT NULL DEFAULT 1 CHECK(level >= 1), xp INTEGER NOT NULL DEFAULT 0 CHECK(xp >= 0), play_time INTEGER NOT NULL DEFAULT 0, battles INTEGER NOT NULL DEFAULT 0, wins INTEGER NOT NULL DEFAULT 0, losses INTEGER NOT NULL DEFAULT 0, tutorial_step INTEGER NOT NULL DEFAULT 0, tutorial_completed INTEGER NOT NULL DEFAULT 0)`,
  `CREATE TABLE IF NOT EXISTS user_settings (user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE, control_mode TEXT NOT NULL DEFAULT 'AUTO' CHECK(control_mode IN ('AUTO','MANUAL')), beginner_mode INTEGER NOT NULL DEFAULT 1, reduced_motion INTEGER NOT NULL DEFAULT 0, preferences_json TEXT NOT NULL DEFAULT '{}', updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS wallets (user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE, credits INTEGER NOT NULL DEFAULT 0 CHECK(credits >= 0), gems INTEGER NOT NULL DEFAULT 0 CHECK(gems >= 0), tickets INTEGER NOT NULL DEFAULT 0 CHECK(tickets >= 0), fragments INTEGER NOT NULL DEFAULT 0 CHECK(fragments >= 0), infinite_credits INTEGER NOT NULL DEFAULT 0, infinite_gacha INTEGER NOT NULL DEFAULT 0)`,
  `CREATE TABLE IF NOT EXISTS sessions (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, token_hash TEXT NOT NULL UNIQUE, csrf_hash TEXT NOT NULL, created_at TEXT NOT NULL, authenticated_at TEXT NOT NULL, expires_at TEXT NOT NULL, ip TEXT, user_agent TEXT, revoked_at TEXT)`,
  `CREATE TABLE IF NOT EXISTS inventory_items (user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, item_type TEXT NOT NULL, item_id TEXT NOT NULL, quantity INTEGER NOT NULL DEFAULT 1 CHECK(quantity >= 1), source TEXT NOT NULL, acquired_at TEXT NOT NULL, PRIMARY KEY(user_id,item_type,item_id))`,
  `CREATE TABLE IF NOT EXISTS currency_transactions (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, currency TEXT NOT NULL, type TEXT NOT NULL, amount INTEGER NOT NULL, balance_before INTEGER NOT NULL, balance_after INTEGER NOT NULL, source TEXT NOT NULL, metadata_json TEXT NOT NULL DEFAULT '{}', created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS xp_transactions (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, amount INTEGER NOT NULL, xp_before INTEGER NOT NULL, xp_after INTEGER NOT NULL, level_before INTEGER NOT NULL, level_after INTEGER NOT NULL, source TEXT NOT NULL, metadata_json TEXT NOT NULL DEFAULT '{}', created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS purchases (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, shop_item_id TEXT NOT NULL, price INTEGER NOT NULL, currency TEXT NOT NULL, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS gacha_pity (user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, banner_id TEXT NOT NULL, pulls INTEGER NOT NULL DEFAULT 0, since_high INTEGER NOT NULL DEFAULT 0, guarantee_featured INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL, PRIMARY KEY(user_id,banner_id))`,
  `CREATE TABLE IF NOT EXISTS gacha_pulls (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, banner_id TEXT NOT NULL, item_type TEXT NOT NULL, item_id TEXT NOT NULL, rarity TEXT NOT NULL, duplicate INTEGER NOT NULL DEFAULT 0, compensation INTEGER NOT NULL DEFAULT 0, pull_index INTEGER NOT NULL, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS battle_history (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, mode TEXT NOT NULL, won INTEGER NOT NULL, duration REAL NOT NULL, manual INTEGER NOT NULL, seed TEXT, reward_xp INTEGER NOT NULL, reward_credits INTEGER NOT NULL, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS builds (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, name TEXT NOT NULL, data_json TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, UNIQUE(user_id,name))`,
  `CREATE TABLE IF NOT EXISTS tutorial_progress (user_id TEXT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE, step INTEGER NOT NULL DEFAULT 0, completed INTEGER NOT NULL DEFAULT 0, viewed_json TEXT NOT NULL DEFAULT '[]', updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS login_attempts (id TEXT PRIMARY KEY, identity_hash TEXT NOT NULL, ip TEXT, success INTEGER NOT NULL, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS password_resets (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, token_hash TEXT NOT NULL UNIQUE, expires_at TEXT NOT NULL, used_at TEXT, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS admin_audit_logs (id TEXT PRIMARY KEY, admin_user_id TEXT NOT NULL REFERENCES users(id), affected_user_id TEXT REFERENCES users(id), action TEXT NOT NULL, before_json TEXT, after_json TEXT, reason TEXT, ip TEXT, created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS migrations (id TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, source_version INTEGER NOT NULL, summary_json TEXT NOT NULL, created_at TEXT NOT NULL, UNIQUE(user_id,source_version))`,
  `CREATE INDEX IF NOT EXISTS sessions_user_idx ON sessions(user_id,expires_at)`,
  `CREATE INDEX IF NOT EXISTS transactions_user_idx ON currency_transactions(user_id,created_at DESC)`,
  `CREATE INDEX IF NOT EXISTS pulls_user_idx ON gacha_pulls(user_id,created_at DESC)`,
  `CREATE INDEX IF NOT EXISTS audit_created_idx ON admin_audit_logs(created_at DESC)`
];

